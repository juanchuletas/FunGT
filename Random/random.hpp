#if !defined(_RANDOM_H_)
#define _RANDOM_H_
#include <random>  
#include <cstdlib>

    float randomFloat(int seed);
    float random(float min, float max);
    float randomFloat(); 
#endif // _RANDOM_H_
